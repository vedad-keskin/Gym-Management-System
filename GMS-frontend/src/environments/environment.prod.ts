export const environment = {
  production: true,
  // Set this to your Render backend URL after deploy (no trailing slash; add / for API calls in config)
  apiUrl: 'https://YOUR-RENDER-SERVICE.onrender.com/'
};
